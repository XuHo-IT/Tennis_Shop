@model TennisShop.Controllers.AdminDashboardViewModel
@{
    ViewData["Title"] = "Dashboard";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";

    var completionRate = Model.TotalOrders > 0 ? (double)Model.CompletedOrders / Model.TotalOrders * 100 : 0;
    var pendingRate = Model.TotalOrders > 0 ? (double)Model.PendingOrders / Model.TotalOrders * 100 : 0;
    var avgOrderValue = Model.TotalOrders > 0 ? Model.TotalRevenue / Model.TotalOrders : 0;

    var revenueTarget = Math.Max((double)Model.TotalRevenue * 1.2, 25000d);
    var revenueProgress = revenueTarget <= 0 ? 0 : Math.Min(100, (double)Model.TotalRevenue / revenueTarget * 100);

    var ordersGoal = Math.Max(Model.TotalOrders * 1.2, 40d);
    var ordersProgress = ordersGoal <= 0 ? 0 : Math.Min(100, (double)Model.TotalOrders / ordersGoal * 100);

    var usersGoal = Math.Max(Model.TotalUsers * 1.2, 20d);
    var usersProgress = usersGoal <= 0 ? 0 : Math.Min(100, (double)Model.TotalUsers / usersGoal * 100);

    var completionPercent = $"{completionRate:F0}%";
    var pendingPercent = $"{pendingRate:F0}%";
    var revenueProgressPercent = $"{revenueProgress:F0}%";
    var ordersProgressPercent = $"{ordersProgress:F0}%";
    var usersProgressPercent = $"{usersProgress:F0}%";
    var adminName = (User.Identity?.Name?.Split(' ').FirstOrDefault() ?? "Admin");
}

<div class="container-fluid">
    <!-- Hero Section -->
    <div class="dashboard-hero mb-5">
        <span class="hero-badge">
            <i class="fas fa-crown me-2"></i>Command Center
        </span>
        <h1>Chào mừng trở lại, @adminName! 👋</h1>
        <p class="hero-description">
            Giám sát toàn bộ hoạt động kinh doanh của bạn tại một nơi. Theo dõi doanh thu, đơn hàng và sự phát triển của khách hàng theo thời gian thực.
        </p>
        <div class="hero-meta">
            <div class="meta-item">
                <span>Tổng Doanh Thu</span>
                <strong>$@Model.TotalRevenue.ToString("N0")</strong>
            </div>
            <div class="meta-item">
                <span>Tổng Đơn Hàng</span>
                <strong>@Model.TotalOrders</strong>
            </div>
            <div class="meta-item">
                <span>Đơn Đang Chờ</span>
                <strong>@Model.PendingOrders</strong>
            </div>
            <div class="meta-item">
                <span>Người Dùng</span>
                <strong>@Model.TotalUsers</strong>
            </div>
        </div>
    </div>

    <!-- Insight Grid -->
    <div class="row g-4 mb-5">
        <div class="col-lg-4">
            <div class="insight-card">
                <div class="insight-card-header">
                    <div>
                        <p class="insight-label">Tình Trạng Doanh Thu</p>
                        <h3 class="text-gradient-primary">$@Model.TotalRevenue.ToString("N0")</h3>
                    </div>
                    <span class="insight-pill success">
                        <i class="fas fa-arrow-up me-1"></i>Ổn định
                    </span>
                </div>
                <p class="insight-description">
                    Tổng thu nhập so với mục tiêu doanh thu hiện tại của bạn.
                </p>
                <div class="insight-progress">
                    <div class="progress-track">
                        <div class="progress-fill" style="width:@revenueProgressPercent"></div>
                    </div>
                    <div class="progress-meta">
                        <span>Mục tiêu: $@revenueTarget.ToString("N0")</span>
                        <strong>@revenueProgressPercent</strong>
                    </div>
                </div>
                <ul class="insight-list">
                    <li>
                        <span>Đơn hoàn thành</span>
                        <strong>@Model.CompletedOrders</strong>
                    </li>
                    <li>
                        <span>Giá trị TB</span>
                        <strong>$@avgOrderValue.ToString("N0")</strong>
                    </li>
                </ul>
            </div>
        </div>

        <div class="col-lg-4">
            <div class="insight-card">
                <div class="insight-card-header">
                    <div>
                        <p class="insight-label">Quy Trình Đơn Hàng</p>
                        <h3 class="text-gradient-primary">@Model.TotalOrders</h3>
                    </div>
                    <span class="insight-pill warning">
                        <i class="fas fa-hourglass-half me-1"></i>@Model.PendingOrders chờ
                    </span>
                </div>
                <p class="insight-description">
                    Theo dõi tiến độ hoàn thành và phản ứng trước các tắc nghẽn.
                </p>
                <div class="insight-progress">
                    <div class="progress-track">
                        <div class="progress-fill neutral" style="width:@ordersProgressPercent"></div>
                    </div>
                    <div class="progress-meta">
                        <span>Mục tiêu: @ordersGoal.ToString("N0") đơn</span>
                        <strong>@ordersProgressPercent</strong>
                    </div>
                </div>
                <ul class="insight-list">
                    <li>
                        <span>Tỷ lệ hoàn thành</span>
                        <strong>@completionPercent</strong>
                    </li>
                    <li>
                        <span>Đang chờ xử lý</span>
                        <strong>@Model.PendingOrders</strong>
                    </li>
                </ul>
            </div>
        </div>

        <div class="col-lg-4">
            <div class="insight-card">
                <div class="insight-card-header">
                    <div>
                        <p class="insight-label">Cơ Sở Khách Hàng</p>
                        <h3 class="text-gradient-primary">@Model.TotalUsers</h3>
                    </div>
                    <span class="insight-pill info">
                        <i class="fas fa-users me-1"></i>Hoạt động
                    </span>
                </div>
                <p class="insight-description">
                    Tất cả các thành viên đã đăng ký bao gồm cả tài khoản quản trị.
                </p>
                <div class="insight-progress">
                    <div class="progress-track">
                        <div class="progress-fill info" style="width:@usersProgressPercent"></div>
                    </div>
                    <div class="progress-meta">
                        <span>Mục tiêu: @usersGoal.ToString("N0") người</span>
                        <strong>@usersProgressPercent</strong>
                    </div>
                </div>
                <ul class="insight-list">
                    <li>
                        <span>Mới gần đây</span>
                        <strong>@Model.RecentUsers.Count</strong>
                    </li>
                    <li>
                        <span>Quản trị viên</span>
                        <strong>@Model.RecentUsers.Count(u => u.Role?.Name == "Admin")</strong>
                    </li>
                </ul>
            </div>
        </div>
    </div>

    <!-- Metric Snapshot -->
    <div class="row g-4 mb-5">
        <div class="col-lg-4 col-md-6">
            <div class="metric-card">
                <div class="metric-icon warning">
                    <i class="fas fa-hourglass-half"></i>
                </div>
                <div class="metric-content">
                    <p class="metric-label">Đơn Hàng Đang Chờ</p>
                    <h4 class="stat-value">@Model.PendingOrders</h4>
                    <span class="metric-trend warning">
                        <i class="fas fa-exclamation-triangle me-1"></i>Cần xem xét
                    </span>
                </div>
            </div>
        </div>
        <div class="col-lg-4 col-md-6">
            <div class="metric-card">
                <div class="metric-icon success">
                    <i class="fas fa-check-double"></i>
                </div>
                <div class="metric-content">
                    <p class="metric-label">Đơn Hàng Hoàn Thành</p>
                    <h4 class="stat-value">@Model.CompletedOrders</h4>
                    <span class="metric-trend positive">
                        <i class="fas fa-check-circle me-1"></i>@completionPercent hoàn tất
                    </span>
                </div>
            </div>
        </div>
        <div class="col-lg-4 col-md-12">
            <div class="metric-card">
                <div class="metric-icon neutral">
                    <i class="fas fa-dollar-sign"></i>
                </div>
                <div class="metric-content">
                    <p class="metric-label">Giá Trị Đơn TB</p>
                    <h4 class="stat-value">$@avgOrderValue.ToString("N2")</h4>
                    <span class="metric-trend neutral">
                        <i class="fas fa-chart-line me-1"></i>Trên @Model.TotalOrders đơn
                    </span>
                </div>
            </div>
        </div>
    </div>

    <div class="section-divider"></div>

    <!-- Quick Actions -->
    <div class="section-header mb-4">
        <h3 class="section-title">
            <i class="fas fa-bolt me-2"></i>Hành Động Nhanh
        </h3>
        <p class="section-subtitle">Các lối tắt đến các tác vụ thường dùng nhất</p>
    </div>
    
    <div class="row g-4 mb-5">
        <div class="col-lg-3 col-md-6">
            <a asp-action="Orders" class="modern-action-card">
                <div class="action-icon-bg orders-bg">
                    <i class="fas fa-shopping-cart"></i>
                </div>
                <div class="action-info">
                    <h5>Quản Lý Đơn Hàng</h5>
                    <p>Xem và xử lý đơn</p>
                </div>
                <div class="action-arrow">
                    <i class="fas fa-arrow-right"></i>
                </div>
            </a>
        </div>
        
        <div class="col-lg-3 col-md-6">
            <a asp-action="Products" class="modern-action-card">
                <div class="action-icon-bg products-bg">
                    <i class="fas fa-box"></i>
                </div>
                <div class="action-info">
                    <h5>Quản Lý Sản Phẩm</h5>
                    <p>Cập nhật kho hàng</p>
                </div>
                <div class="action-arrow">
                    <i class="fas fa-arrow-right"></i>
                </div>
            </a>
        </div>
        
        <div class="col-lg-3 col-md-6">
            <a asp-action="CreateProduct" class="modern-action-card">
                <div class="action-icon-bg add-bg">
                    <i class="fas fa-plus-circle"></i>
                </div>
                <div class="action-info">
                    <h5>Thêm Sản Phẩm</h5>
                    <p>Tạo sản phẩm mới</p>
                </div>
                <div class="action-arrow">
                    <i class="fas fa-arrow-right"></i>
                </div>
            </a>
        </div>
        
        <div class="col-lg-3 col-md-6">
            <a asp-action="Users" class="modern-action-card">
                <div class="action-icon-bg users-bg">
                    <i class="fas fa-users-cog"></i>
                </div>
                <div class="action-info">
                    <h5>Quản Lý Người Dùng</h5>
                    <p>Quản lý tài khoản</p>
                </div>
                <div class="action-arrow">
                    <i class="fas fa-arrow-right"></i>
                </div>
            </a>
        </div>
    </div>

    <!-- Recent Activity -->
    <div class="section-header mb-4">
        <h3 class="section-title">
            <i class="fas fa-clock me-2"></i>Hoạt Động Gần Đây
        </h3>
        <p class="section-subtitle">Cập nhật mới nhất từ cửa hàng của bạn</p>
    </div>
    
    <div class="row g-4">
        <!-- Recent Orders -->
        <div class="col-lg-4">
            <div class="modern-activity-card">
                <div class="activity-card-header">
                    <div class="header-icon orders-icon">
                        <i class="fas fa-shopping-cart"></i>
                    </div>
                    <div class="header-info">
                        <h6>Đơn Hàng Mới Nhất</h6>
                        <p>@Model.RecentOrders.Count đơn gần đây</p>
                    </div>
                    <a asp-action="Orders" class="header-link">
                        <i class="fas fa-arrow-right"></i>
                    </a>
                </div>
                <div class="activity-body">
                    @if (Model.RecentOrders.Any())
                    {
                        @foreach (var order in Model.RecentOrders)
                        {
                            <div class="activity-item">
                                <div class="activity-avatar bg-primary">
                                    <i class="fas fa-receipt"></i>
                                </div>
                                <div class="activity-details">
                                    <div class="activity-title">Order #@order.Id</div>
                                    <div class="activity-subtitle">@(order.User?.FullName ?? "Guest")</div>
                                    <div class="activity-meta">
                                        <span class="activity-price">$@order.TotalAmount?.ToString("F2")</span>
                                        <span class="activity-time">@order.OrderDate?.ToString("MMM dd")</span>
                                    </div>
                                </div>
                                <div class="activity-status">
                                    @{
                                        var statusClass = order.Status?.ToLower() switch
                                        {
                                            "pending" => "status-warning",
                                            "processing" => "status-info",
                                            "completed" => "status-success",
                                            "shipped" => "status-primary",
                                            _ => "status-secondary"
                                        };
                                    }
                                    <span class="status-badge @statusClass">@order.Status</span>
                                </div>
                            </div>
                        }
                    }
                    else
                    {
                        <div class="empty-state">
                            <i class="fas fa-inbox fa-2x mb-3"></i>
                            <p>Chưa có đơn hàng nào</p>
                        </div>
                    }
                </div>
            </div>
        </div>

        <!-- Recent Users -->
        <div class="col-lg-4">
            <div class="modern-activity-card">
                <div class="activity-card-header">
                    <div class="header-icon users-icon">
                        <i class="fas fa-users"></i>
                    </div>
                    <div class="header-info">
                        <h6>Người Dùng Mới</h6>
                        <p>@Model.RecentUsers.Count đăng ký mới</p>
                    </div>
                    <a asp-action="Users" class="header-link">
                        <i class="fas fa-arrow-right"></i>
                    </a>
                </div>
                <div class="activity-body">
                    @if (Model.RecentUsers.Any())
                    {
                        @foreach (var user in Model.RecentUsers)
                        {
                            <div class="activity-item">
                                <div class="activity-avatar bg-success">
                                    @{
                                        var initials = string.Join("", (user.FullName ?? "U").Split(' ', StringSplitOptions.RemoveEmptyEntries).Take(2).Select(s => s[0])).ToUpper();
                                    }
                                    @initials
                                </div>
                                <div class="activity-details">
                                    <div class="activity-title">@user.FullName</div>
                                    <div class="activity-subtitle">@user.Email</div>
                                    <div class="activity-meta">
                                        <span class="activity-time">
                                            <i class="far fa-clock me-1"></i>@user.CreatedAt?.ToString("MMM dd, yyyy")
                                        </span>
                                    </div>
                                </div>
                                <div class="activity-status">
                                    <span class="status-badge @(user.Role?.Name == "Admin" ? "status-danger" : "status-primary")">
                                        @(user.Role?.Name ?? "User")
                                    </span>
                                </div>
                            </div>
                        }
                    }
                    else
                    {
                        <div class="empty-state">
                            <i class="fas fa-user-slash fa-2x mb-3"></i>
                            <p>Chưa có người dùng mới</p>
                        </div>
                    }
                </div>
            </div>
        </div>

        <!-- Recent Products -->
        <div class="col-lg-4">
            <div class="modern-activity-card">
                <div class="activity-card-header">
                    <div class="header-icon products-icon">
                        <i class="fas fa-box"></i>
                    </div>
                    <div class="header-info">
                        <h6>Sản Phẩm Mới</h6>
                        <p>@Model.RecentProducts.Count sản phẩm mới</p>
                    </div>
                    <a asp-action="Products" class="header-link">
                        <i class="fas fa-arrow-right"></i>
                    </a>
                </div>
                <div class="activity-body">
                    @if (Model.RecentProducts.Any())
                    {
                        @foreach (var product in Model.RecentProducts)
                        {
                            <div class="activity-item">
                                <div class="activity-avatar bg-info">
                                    @if (product.ProductImages != null && product.ProductImages.Any())
                                    {
                                        <img src="@product.ProductImages.First().ImageUrl" alt="@product.Name" />
                                    }
                                    else
                                    {
                                        <i class="fas fa-box"></i>
                                    }
                                </div>
                                <div class="activity-details">
                                    <div class="activity-title">@product.Name</div>
                                    <div class="activity-subtitle">@(product.Category?.Name ?? "Uncategorized")</div>
                                    <div class="activity-meta">
                                        <span class="activity-price">$@product.BasePrice.ToString("F2")</span>
                                        <span class="activity-time">
                                            <i class="fas fa-box me-1"></i>@product.Stock
                                        </span>
                                    </div>
                                </div>
                                <div class="activity-status">
                                    <span class="status-badge @(product.IsActive == true ? "status-success" : "status-secondary")">
                                        @(product.IsActive == true ? "Active" : "Inactive")
                                    </span>
                                </div>
                            </div>
                        }
                    }
                    else
                    {
                        <div class="empty-state">
                            <i class="fas fa-box-open fa-2x mb-3"></i>
                            <p>Chưa có sản phẩm mới</p>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        // Animate stat values on page load
        document.querySelectorAll('.stat-value').forEach((element) => {
            element.style.opacity = '0';
            element.style.transform = 'translateY(10px)';
            setTimeout(() => {
                element.style.transition = 'all 0.5s ease';
                element.style.opacity = '1';
                element.style.transform = 'translateY(0)';
            }, 100);
        });
        
        // Add hover effect to activity items
        document.querySelectorAll('.activity-item').forEach(item => {
            item.addEventListener('mouseenter', function() {
                this.style.transform = 'translateX(4px)';
            });
            item.addEventListener('mouseleave', function() {
                this.style.transform = 'translateX(0)';
            });
        });
    </script>
}
